{
  "version": "0.2.0",
  "configurations": [{
    "name": "Run Code X-Ray (demo)", "type": "extensionHost", "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/examples/demo"],
    "outFiles": ["${workspaceFolder}/dist/**/*.cjs"], "preLaunchTask": "npm: build"
  }]
}
