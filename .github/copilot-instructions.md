Follow AGENTS.md at repository root. Read docs/ARCHITECTURE.md and docs/ROADMAP.md before implementation. Keep src/core independent of VS Code and the DOM; webview imports only shared types. Never execute analyzed project code. Preserve traceable evidence and explicit coverage limitations. Verify with npm run check and document actual Extension Development Host checks separately from unit tests. Use Czech explanations for the project owner.
