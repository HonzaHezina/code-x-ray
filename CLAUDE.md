# Claude Code onboarding

Read and follow `AGENTS.md` before making changes. Then read `START_HERE_CS.md`, `docs/ARCHITECTURE.md`, `docs/ROADMAP.md`, and `docs/VALIDATION.md`.

Run `npm ci` and `npm run check`. Keep core, extension and webview separate. This is a VS Code extension with a local analyzer, not a web service. The first follow-up task is real VS Code host validation, not a rewrite.
